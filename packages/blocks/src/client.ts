'use client';

/**
 * @orion-ds/blocks/client
 *
 * Client-side entry point for Next.js App Router
 * Re-exports all sections and templates with 'use client' directive
 *
 * Use this entry point in Next.js when you need client-side state or effects:
 *
 * @example
 * ```tsx
 * // app/page.tsx
 * import { AgentEditor, AgentWorkspace } from '@orion-ds/blocks/client';
 *
 * export default function Page() {
 *   return <AgentEditor />;
 * }
 * ```
 */

export * from './index';
